cmake_minimum_required(VERSION 3.16)

project(Scarlet VERSION 0.1 LANGUAGES CXX)

set(CMAKE_CXX_STANDARD_REQUIRED ON)

find_package(Qt6 REQUIRED COMPONENTS Quick)

qt_standard_project_setup(REQUIRES 6.8)

qt_add_executable(appScarlet
    main.cpp
)

set_source_files_properties(Colors.qml PROPERTIES
    QT_QML_SINGLETON_TYPE TRUE
)

qt_add_qml_module(appScarlet
    URI Scarlet
    VERSION 1.0
    QML_FILES
        Main.qml
        QML_FILES
        QML_FILES ApplicationFlow.qml
        QML_FILES ApplicationFlowForm.ui.qml
        QML_FILES HomeToolBar.ui.qml
        QML_FILES HomeForm.ui.qml
        QML_FILES Home.qml
        QML_FILES CustomButtonForm.ui.qml
        QML_FILES CustomButton.qml
        QML_FILES Colors.qml
    RESOURCES
        images.qrc
        images/what.jpg
        RESOURCES images/profile-icon.svg
        RESOURCES images/sword-icon.svg
        RESOURCES images/theme-dark.svg images/theme-light.svg
        RESOURCES images/lizard.svg
        RESOURCES images/sword-dark.svg

)

# Qt for iOS sets MACOSX_BUNDLE_GUI_IDENTIFIER automatically since Qt 6.1.
# If you are developing for iOS or macOS you should consider setting an
# explicit, fixed bundle identifier manually though.
set_target_properties(appScarlet PROPERTIES
#    MACOSX_BUNDLE_GUI_IDENTIFIER com.example.appScarlet
    MACOSX_BUNDLE_BUNDLE_VERSION ${PROJECT_VERSION}
    MACOSX_BUNDLE_SHORT_VERSION_STRING ${PROJECT_VERSION_MAJOR}.${PROJECT_VERSION_MINOR}
    MACOSX_BUNDLE TRUE
    WIN32_EXECUTABLE TRUE
)

target_link_libraries(appScarlet
    PRIVATE Qt6::Quick
)

include(GNUInstallDirs)
install(TARGETS appScarlet
    BUNDLE DESTINATION .
    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
    RUNTIME DESTINATION ${CMAKE_INSTALL_BINDIR}
)
